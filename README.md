# Live Captions Web App

This is a free offline speech-to-text web app for **live captions**, using Vosk tiny model.

## How to Run

1. Install Python 3.10+ and dependencies:

```bash
pip install -r requirements.txt
